@using YESSMobilePWA.Models

<div class="modal-backdrop fade show"></div>
<div class="modal show d-block" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content bg-dark text-white">
            <div class="modal-header">
                <h5 class="modal-title">Nueva persona</h5>
                <button type="button" class="btn-close btn-close-white" @onclick="Cerrar"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <label class="form-label">Nombre</label>
                    <input type="text" class="form-control" @bind="Formulario.Nombre" />
                </div>
                <div class="mb-3">
                    <label class="form-label">Tipo</label>
                    <select class="form-select" @bind="Formulario.Tipo">
                        <option value="Deudor">Deudor</option>
                        <option value="Acreedor">Acreedor</option>
                        <option value="Ambos">Ambos</option>
                        <option value="Ninguno">Ninguno</option>
                    </select>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" @onclick="Cerrar">Cancelar</button>
                <button type="button" class="btn btn-primary" @onclick="Guardar">Guardar</button>
            </div>
        </div>
    </div>
</div>

@code {
    [Parameter] public EventCallback<Persona> OnPersonaAgregada { get; set; }
    [Parameter] public EventCallback OnCancelar { get; set; }

    private PersonaFormulario Formulario { get; set; } = new();

    private class PersonaFormulario
    {
        public string Nombre { get; set; } = "";
        public string Tipo { get; set; } = "Ninguno";
    }

    private async Task Guardar()
    {
        if (string.IsNullOrWhiteSpace(Formulario.Nombre))
            return;
        var nueva = new Persona(Formulario.Nombre, Formulario.Tipo);
        await OnPersonaAgregada.InvokeAsync(nueva);
        Cerrar();
    }

    private void Cerrar()
    {
        OnCancelar.InvokeAsync();
    }
}