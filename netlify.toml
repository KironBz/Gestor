[build]
  base = "YESS MV/YESSMobilePWA"
  command = "curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --version 10.0.200 --install-dir /opt/dotnet10 && export PATH=/opt/dotnet10:$PATH && dotnet publish -c Release -o publish"
  publish = "publish/wwwroot"